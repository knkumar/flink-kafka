# Claim-to-Evidence Map

Current date checked: `2026-07-15T15:46:48-07:00`.

| Claim | Status | Evidence |
| --- | --- | --- |
| The repository contains executable definitions for five proposed workloads. | Supported | `src/stream_state_bench/workloads.py` defines identity, filter/map, tumbling count, sliding sum, and stream-stream join. |
| The workload and verifier behavior has unit coverage. | Supported | `make check` ran `python3 -m unittest discover -s tests`; 20 tests passed. |
| The repository can verify external engine output files against the reference semantics. | Supported | `src/stream_state_bench/verify_external_output.py` loads JSONL output records and compares them to `reference_outputs`; `tests/test_external_output.py` covers loading and a passing identity comparison. |
| A local deterministic experiment was executed for all five workloads. | Supported | `experiments/results/local_semantic_results.json` contains five results with `events: 1000`, `keys: 100`, and `seed: 7`. |
| The local semantic harness found no missing, unexpected, or duplicate records for the generated run. | Supported | In `experiments/results/local_semantic_results.json`, every workload has `missing_count: 0`, `unexpected_count: 0`, `duplicate_count: 0`, and `passed: true`. |
| Kafka Streams 4.3.1 produced correct W1 identity output for the generated 1000-record run. | Supported | `experiments/results/kafka_streams_w1/verification.json` has `expected_count: 1000`, `actual_count: 1000`, zero missing, zero unexpected, zero duplicates, and `passed: true`. |
| Flink 2.2.0 produced correct W1 identity output for the generated 1000-record run. | Supported | `experiments/results/flink_w1/verification.json` has `expected_count: 1000`, `actual_count: 1000`, zero missing, zero unexpected, zero duplicates, and `passed: true`. |
| Kafka Streams 4.3.1 produced correct W2 filter/map output for the generated 1000-record run. | Supported | `experiments/results/kafka_streams_w2/verification.json` has `expected_count: 513`, `actual_count: 513`, zero missing, zero unexpected, zero duplicates, and `passed: true`. |
| Flink 2.2.0 produced correct W2 filter/map output for the generated 1000-record run. | Supported | `experiments/results/flink_w2/verification.json` has `expected_count: 513`, `actual_count: 513`, zero missing, zero unexpected, zero duplicates, and `passed: true`. |
| Kafka Streams 4.3.1 produced correct W3 tumbling-count output for the generated 1000-record run. | Supported | `experiments/results/kafka_streams_w3/verification.json` has `expected_count: 199`, `actual_count: 199`, zero missing, zero unexpected, zero duplicates, and `passed: true`. |
| Flink 2.2.0 produced correct W3 tumbling-count output for the generated 1000-record run. | Supported | `experiments/results/flink_w3/verification.json` has `expected_count: 199`, `actual_count: 199`, zero missing, zero unexpected, zero duplicates, and `passed: true`. |
| Kafka Streams 4.3.1 produced correct W4 sliding-sum output for the generated 1000-record run with `start_ms: 600000`. | Supported | `experiments/results/kafka_streams_w4/verification.json` has `expected_count: 1099`, `actual_count: 1099`, zero missing, zero unexpected, zero duplicates, and `passed: true`. |
| Flink 2.2.0 produced correct W4 sliding-sum output for the generated 1000-record run with `start_ms: 600000`. | Supported | `experiments/results/flink_w4/verification.json` has `expected_count: 1099`, `actual_count: 1099`, zero missing, zero unexpected, zero duplicates, and `passed: true`. |
| Kafka Streams 4.3.1 produced correct W5 stream-stream join output for the generated paired 1000-left/1000-right run with `start_ms: 1000`. | Supported | `experiments/results/kafka_streams_w5/verification.json` has `expected_count: 11024`, `actual_count: 11024`, zero missing, zero unexpected, zero duplicates, and `passed: true`. |
| Flink 2.2.0 produced correct W5 stream-stream join output for the generated paired 1000-left/1000-right run with `start_ms: 1000`. | Supported | `experiments/results/flink_w5/verification.json` has `expected_count: 11024`, `actual_count: 11024`, zero missing, zero unexpected, zero duplicates, and `passed: true`. |
| Kafka Streams 4.3.1 and Flink 2.2.0 each passed two W1-W5 correctness executions. | Supported | `experiments/results/engine_correctness_summary.md` has 20 passing rows: baseline and `repeat1` for W1-W5 across both engines. Every row reports zero missing, zero unexpected, and zero duplicate records. |
| Kafka Streams 4.3.1 produced correct W1 identity output during one fixed-rate host-side latency probe. | Supported | `experiments/results/kafka_streams_w1_latency/verification.json` has `expected_count: 100`, `actual_count: 100`, zero missing, zero unexpected, zero duplicates, and `passed: true` for the 20 records/sec probe. |
| The Kafka Streams W1 fixed-rate probe measured a host-side p99 read-committed visibility-delay proxy of 2831.634 ms. | Supported with scope limit | `experiments/results/kafka_streams_w1_latency/latency_summary.json` reports `measurement: host_write_to_read_committed_visibility_delay_proxy`, `rate_per_sec: 20.0`, `matched_records: 100`, `p50_ms: 1332.012`, `p95_ms: 2631.678`, and `p99_ms: 2831.634`. Calibrated T0-T3 measurement is now supported. W1 sweep and repeat rows below define the current rate coverage. |
| Kafka Streams W1 identity has a first host-side rate sweep at 10, 20, and 40 records/sec. | Supported with scope limit | `experiments/results/latency_summary.md` reports Kafka Streams W1 passing runs at 10/sec, 20/sec, and 40/sec. The p99 values are 2734.456 ms, 2831.634 ms, and 2928.505 ms. This is a 100-record local proxy sweep, not a throughput envelope. |
| Kafka Streams W1 identity has two accepted 20 records/sec host-side latency runs. | Supported with scope limit | `experiments/results/latency_aggregate_summary.md` reports two Kafka Streams W1 runs at 20/sec with all runs passed, p99 min 2831.634 ms, p99 max 2833.992 ms, and mean p99 2832.813 ms. |
| Flink 2.2.0 produced correct W1 identity output during one fixed-rate host-side latency probe. | Supported | `experiments/results/flink_w1_latency/verification.json` has `expected_count: 100`, `actual_count: 100`, zero missing, zero unexpected, zero duplicates, and `passed: true` for the 20 records/sec probe. |
| The Flink W1 fixed-rate probe measured a host-side p99 read-committed visibility-delay proxy of 2593.946 ms. | Supported with scope limit | `experiments/results/flink_w1_latency/latency_summary.json` reports `measurement: host_write_to_read_committed_visibility_delay_proxy`, `rate_per_sec: 20.0`, `matched_records: 100`, `p50_ms: 951.127`, `p95_ms: 2393.965`, and `p99_ms: 2593.946`. Calibrated T0-T3 measurement is now supported. W1 sweep and repeat rows below define the current rate coverage. |
| Flink W1 identity has a first host-side rate sweep at 10, 20, and 40 records/sec. | Supported with scope limit | `experiments/results/latency_summary.md` reports Flink W1 passing runs at 10/sec, 20/sec, and 40/sec. The p99 values are 2460.697 ms, 2593.946 ms, and 3214.316 ms. This is a 100-record local proxy sweep, not a throughput envelope. |
| Flink W1 identity has two accepted 20 records/sec host-side latency runs. | Supported with scope limit | `experiments/results/latency_aggregate_summary.md` reports two Flink W1 runs at 20/sec with all runs passed, p99 min 2593.946 ms, p99 max 2647.962 ms, and mean p99 2620.954 ms. |
| Kafka Streams 4.3.1 produced correct W2 filter/map output during one fixed-rate host-side latency probe. | Supported | `experiments/results/kafka_streams_w2_latency/verification.json` has `expected_count: 56`, `actual_count: 56`, zero missing, zero unexpected, zero duplicates, and `passed: true` for the 20 records/sec probe. |
| The Kafka Streams W2 fixed-rate probe measured a host-side p99 read-committed visibility-delay proxy of 2993.158 ms. | Supported with scope limit | `experiments/results/kafka_streams_w2_latency/latency_summary.json` reports `measurement: host_write_to_read_committed_visibility_delay_proxy`, `rate_per_sec: 20.0`, `produced_records: 100`, `expected_output_records: 56`, `matched_records: 56`, `p50_ms: 1226.708`, `p95_ms: 2843.058`, and `p99_ms: 2993.158`. Calibrated T0-T3 measurement is now supported. W2 sweep and repeat rows below define the current rate coverage. |
| Kafka Streams W2 filter/map has a first host-side rate sweep at 10, 20, and 40 records/sec. | Supported with scope limit | `experiments/results/latency_summary.md` reports Kafka Streams W2 passing runs at 10/sec, 20/sec, and 40/sec. The p99 values are 2825.823 ms, 2993.158 ms, and 2964.982 ms. This is a 100-input local proxy sweep, not a throughput envelope. |
| Kafka Streams W2 filter/map has two accepted 20 records/sec host-side latency runs. | Supported with scope limit | `experiments/results/latency_aggregate_summary.md` reports two Kafka Streams W2 runs at 20/sec with all runs passed, p99 min 2814.333 ms, p99 max 2993.158 ms, and mean p99 2903.745 ms. |
| Flink 2.2.0 produced correct W2 filter/map output during one fixed-rate host-side latency probe. | Supported | `experiments/results/flink_w2_latency/verification.json` has `expected_count: 56`, `actual_count: 56`, zero missing, zero unexpected, zero duplicates, and `passed: true` for the 20 records/sec probe. |
| The Flink W2 fixed-rate probe measured a host-side p99 read-committed visibility-delay proxy of 3350.702 ms. | Supported with scope limit | `experiments/results/flink_w2_latency/latency_summary.json` reports `measurement: host_write_to_read_committed_visibility_delay_proxy`, `rate_per_sec: 20.0`, `produced_records: 100`, `expected_output_records: 56`, `matched_records: 56`, `p50_ms: 1400.690`, `p95_ms: 3200.614`, and `p99_ms: 3350.702`. Calibrated T0-T3 measurement is now supported. W2 sweep and repeat rows below define the current rate coverage. |
| Flink W2 filter/map has a first host-side rate sweep at 10, 20, and 40 records/sec. | Supported with scope limit | `experiments/results/latency_summary.md` reports Flink W2 passing runs at 10/sec, 20/sec, and 40/sec. The p99 values are 2632.060 ms, 3350.702 ms, and 3287.256 ms. This is a 100-input local proxy sweep, not a throughput envelope. |
| Flink W2 filter/map has two accepted 20 records/sec host-side latency runs. | Supported with scope limit | `experiments/results/latency_aggregate_summary.md` reports two Flink W2 runs at 20/sec with all runs passed, p99 min 2761.075 ms, p99 max 3350.702 ms, and mean p99 3055.889 ms. |
| Kafka Streams 4.3.1 produced correct W3 tumbling-count output during one fixed-rate host-side latency probe. | Supported | `experiments/results/kafka_streams_w3_latency/verification.json` has `expected_count: 71`, `actual_count: 71`, zero missing, zero unexpected, zero duplicates, and `passed: true` for the 20 records/sec probe with 100 benchmark records plus one tick. |
| The Kafka Streams W3 fixed-rate probe measured a host-side p99 read-committed visibility-delay proxy of 7123.280 ms. | Supported with scope limit | `experiments/results/kafka_streams_w3_latency/latency_summary.json` reports `measurement: host_write_to_read_committed_visibility_delay_proxy`, `rate_per_sec: 20.0`, `produced_records: 101`, `expected_output_records: 71`, `matched_records: 71`, `p50_ms: 3973.097`, `p95_ms: 6873.202`, and `p99_ms: 7123.280`. For W3 the proxy uses the latest sent contributing benchmark input event for each final window output. Calibrated T0-T3 measurement is now supported. W3 sweep and repeat rows below define the current rate coverage. |
| Kafka Streams W3 tumbling count has a first host-side rate sweep at 10, 20, and 40 records/sec. | Supported with scope limit | `experiments/results/latency_summary.md` reports Kafka Streams W3 passing runs at 10/sec, 20/sec, and 40/sec. The p99 values are 11631.267 ms, 7123.280 ms, and 3942.612 ms. This is a 100-input plus one-tick local proxy sweep, not a throughput envelope. |
| Kafka Streams W3 tumbling count has two accepted 20 records/sec host-side latency runs. | Supported with scope limit | `experiments/results/latency_aggregate_summary.md` reports two Kafka Streams W3 runs at 20/sec with all runs passed, p99 min 6744.307 ms, p99 max 7123.280 ms, and mean p99 6933.793 ms. |
| Flink 2.2.0 produced correct W3 tumbling-count output during one fixed-rate host-side latency probe. | Supported | `experiments/results/flink_w3_latency/verification.json` has `expected_count: 71`, `actual_count: 71`, zero missing, zero unexpected, zero duplicates, and `passed: true` for the 20 records/sec probe with 100 benchmark records plus one tick. |
| The Flink W3 fixed-rate probe measured a host-side p99 read-committed visibility-delay proxy of 5317.375 ms. | Supported with scope limit | `experiments/results/flink_w3_latency/latency_summary.json` reports `measurement: host_write_to_read_committed_visibility_delay_proxy`, `rate_per_sec: 20.0`, `produced_records: 101`, `expected_output_records: 71`, `matched_records: 71`, `p50_ms: 2167.446`, `p95_ms: 5068.230`, and `p99_ms: 5317.375`. For W3 the proxy uses the latest sent contributing benchmark input event for each final window output. Calibrated T0-T3 measurement is now supported. W3 sweep and repeat rows below define the current rate coverage. |
| Flink W3 tumbling count has a first host-side rate sweep at 10, 20, and 40 records/sec. | Supported with scope limit | `experiments/results/latency_summary.md` reports Flink W3 passing runs at 10/sec, 20/sec, and 40/sec. The p99 values are 10527.388 ms, 5317.375 ms, and 3651.222 ms. This is a 100-input plus one-tick local proxy sweep, not a throughput envelope. |
| Flink W3 tumbling count has two accepted 20 records/sec host-side latency runs. | Supported with scope limit | `experiments/results/latency_aggregate_summary.md` reports two Flink W3 runs at 20/sec with all runs passed, p99 min 5317.375 ms, p99 max 5658.188 ms, and mean p99 5487.782 ms. |
| Kafka Streams 4.3.1 produced correct W4 sliding-sum output during one fixed-rate host-side latency probe. | Supported | `experiments/results/kafka_streams_w4_latency/verification.json` has `expected_count: 710`, `actual_count: 710`, zero missing, zero unexpected, zero duplicates, and `passed: true` for the 20 records/sec probe with 100 benchmark records plus one tick and `start_ms: 600000`. |
| The Kafka Streams W4 fixed-rate probe measured a host-side p99 read-committed visibility-delay proxy of 7113.249 ms. | Supported with scope limit | `experiments/results/kafka_streams_w4_latency/latency_summary.json` reports `measurement: host_write_to_read_committed_visibility_delay_proxy`, `rate_per_sec: 20.0`, `produced_records: 101`, `expected_output_records: 710`, `matched_records: 710`, `p50_ms: 3964.038`, `p95_ms: 6864.071`, and `p99_ms: 7113.249`. For W4 the proxy uses the latest sent contributing benchmark input event for each final sliding-window output. Calibrated T0-T3 measurement is now supported. W4 sweep and repeat rows below define the current rate coverage. |
| Kafka Streams W4 sliding sum has a first host-side rate sweep at 10, 20, and 40 records/sec. | Supported with scope limit | `experiments/results/latency_summary.md` reports Kafka Streams W4 passing runs at 10/sec, 20/sec, and 40/sec. The p99 values are 11164.941 ms, 7113.249 ms, and 4006.722 ms. This is a 100-input plus one-tick local proxy sweep, not a throughput envelope. |
| Kafka Streams W4 sliding sum has two accepted 20 records/sec host-side latency runs. | Supported with scope limit | `experiments/results/latency_aggregate_summary.md` reports two Kafka Streams W4 runs at 20/sec with all runs passed, p99 min 6338.270 ms, p99 max 7113.249 ms, and mean p99 6725.760 ms. |
| Flink 2.2.0 produced correct W4 sliding-sum output during one fixed-rate host-side latency probe. | Supported | `experiments/results/flink_w4_latency/verification.json` has `expected_count: 710`, `actual_count: 710`, zero missing, zero unexpected, zero duplicates, and `passed: true` for the 20 records/sec probe with 100 benchmark records plus one tick and `start_ms: 600000`. |
| The Flink W4 fixed-rate probe measured a host-side p99 read-committed visibility-delay proxy of 5786.186 ms. | Supported with scope limit | `experiments/results/flink_w4_latency/latency_summary.json` reports `measurement: host_write_to_read_committed_visibility_delay_proxy`, `rate_per_sec: 20.0`, `produced_records: 101`, `expected_output_records: 710`, `matched_records: 710`, `p50_ms: 2638.442`, `p95_ms: 5538.424`, and `p99_ms: 5786.186`. For W4 the proxy uses the latest sent contributing benchmark input event for each final sliding-window output. Calibrated T0-T3 measurement is now supported. W4 sweep and repeat rows below define the current rate coverage. |
| Flink W4 sliding sum has a first host-side rate sweep at 10, 20, and 40 records/sec. | Supported with scope limit | `experiments/results/latency_summary.md` reports Flink W4 passing runs at 10/sec, 20/sec, and 40/sec. The p99 values are 10390.270 ms, 5786.186 ms, and 3540.110 ms. This is a 100-input plus one-tick local proxy sweep, not a throughput envelope. |
| Flink W4 sliding sum has two accepted 20 records/sec host-side latency runs. | Supported with scope limit | `experiments/results/latency_aggregate_summary.md` reports two Flink W4 runs at 20/sec with all runs passed, p99 min 5507.883 ms, p99 max 5786.186 ms, and mean p99 5647.034 ms. |
| Kafka Streams 4.3.1 produced correct W5 stream-stream join output during one fixed-rate host-side latency probe. | Supported | `experiments/results/kafka_streams_w5_latency/verification.json` has `expected_count: 186`, `actual_count: 186`, zero missing, zero unexpected, zero duplicates, and `passed: true` for the 20 records/sec probe with 100 left and 100 right input records and `start_ms: 1000`. |
| The Kafka Streams W5 fixed-rate probe measured a host-side p99 read-committed visibility-delay proxy of 4096.137 ms. | Supported with scope limit | `experiments/results/kafka_streams_w5_latency/latency_summary.json` reports `measurement: host_write_to_read_committed_visibility_delay_proxy`, `rate_per_sec: 20.0`, `produced_records: 200`, `expected_output_records: 186`, `matched_records: 186`, `p50_ms: 2544.645`, `p95_ms: 3896.173`, and `p99_ms: 4096.137`. For W5 the proxy uses the latest sent contributing left or right input event for each join output. Calibrated T0-T3 measurement is now supported. W5 sweep and repeat rows below define the current rate coverage. |
| Kafka Streams W5 stream-stream join has a first host-side rate sweep at 10, 20, and 40 records/sec. | Supported with scope limit | `experiments/results/latency_summary.md` reports Kafka Streams W5 passing runs at 10/sec, 20/sec, and 40/sec. The p99 values are 3435.417 ms, 4096.137 ms, and 3438.284 ms. This is a 100-left plus 100-right local proxy sweep, not a throughput envelope. |
| Kafka Streams W5 stream-stream join has two accepted 20 records/sec host-side latency runs. | Supported with scope limit | `experiments/results/latency_aggregate_summary.md` reports two Kafka Streams W5 runs at 20/sec with all runs passed, p99 min 4096.137 ms, p99 max 4098.726 ms, and mean p99 4097.431 ms. |
| Flink 2.2.0 produced correct W5 stream-stream join output during one fixed-rate host-side latency probe. | Supported | `experiments/results/flink_w5_latency/verification.json` has `expected_count: 186`, `actual_count: 186`, zero missing, zero unexpected, zero duplicates, and `passed: true` for the 20 records/sec probe with 100 left and 100 right input records and `start_ms: 1000`. |
| The Flink W5 fixed-rate probe measured a host-side p99 read-committed visibility-delay proxy of 2521.762 ms. | Supported with scope limit | `experiments/results/flink_w5_latency/latency_summary.json` reports `measurement: host_write_to_read_committed_visibility_delay_proxy`, `rate_per_sec: 20.0`, `produced_records: 200`, `expected_output_records: 186`, `matched_records: 186`, `p50_ms: 805.600`, `p95_ms: 2321.768`, and `p99_ms: 2521.762`. For W5 the proxy uses the latest sent contributing left or right input event for each join output. Calibrated T0-T3 measurement is now supported. W5 sweep and repeat rows below define the current rate coverage. |
| Flink W5 stream-stream join has a first host-side rate sweep at 10, 20, and 40 records/sec. | Supported with scope limit | `experiments/results/latency_summary.md` reports Flink W5 passing runs at 10/sec, 20/sec, and 40/sec. The p99 values are 2439.144 ms, 2521.762 ms, and 2805.058 ms. This is a 100-left plus 100-right local proxy sweep, not a throughput envelope. |
| Flink W5 stream-stream join has two accepted 20 records/sec host-side latency runs. | Supported with scope limit | `experiments/results/latency_aggregate_summary.md` reports two Flink W5 runs at 20/sec with all runs passed, p99 min 2521.762 ms, p99 max 3259.235 ms, and mean p99 2890.499 ms. |
| The Kafka Streams W1 run used Kafka in KRaft mode. | Supported | `experiments/results/kafka_streams_w1/docker-compose.log` contains `Running in KRaft mode...`; `experiments/kafka_streams_w1/docker-compose.yml` uses `apache/kafka:4.3.1` with broker and controller roles. |
| The Flink W1 run used Kafka in KRaft mode. | Supported | `experiments/results/flink_w1/docker-compose.log` contains `Running in KRaft mode...`; `experiments/flink_w1/docker-compose.yml` uses `apache/kafka:4.3.1` with broker and controller roles. |
| The Kafka Streams W2 run used Kafka in KRaft mode. | Supported | `experiments/results/kafka_streams_w2/docker-compose.log` contains `Running in KRaft mode...`; `experiments/kafka_streams_w1/docker-compose.yml` uses `apache/kafka:4.3.1` with broker and controller roles. |
| The Flink W2 run used Kafka in KRaft mode. | Supported | `experiments/results/flink_w2/docker-compose.log` contains `Running in KRaft mode...`; `experiments/flink_w1/docker-compose.yml` uses `apache/kafka:4.3.1` with broker and controller roles. |
| The Kafka Streams W3 run used Kafka in KRaft mode. | Supported | `experiments/results/kafka_streams_w3/docker-compose.log` contains `Running in KRaft mode...`; `experiments/kafka_streams_w1/docker-compose.yml` uses `apache/kafka:4.3.1` with broker and controller roles. |
| The Flink W3 run used Kafka in KRaft mode. | Supported | `experiments/results/flink_w3/docker-compose.log` contains `Running in KRaft mode...`; `experiments/flink_w1/docker-compose.yml` uses `apache/kafka:4.3.1` with broker and controller roles. |
| The Kafka Streams W4 run used Kafka in KRaft mode. | Supported | `experiments/results/kafka_streams_w4/docker-compose.log` contains `Running in KRaft mode...`; `experiments/kafka_streams_w1/docker-compose.yml` uses `apache/kafka:4.3.1` with broker and controller roles. |
| The Flink W4 run used Kafka in KRaft mode. | Supported | `experiments/results/flink_w4/docker-compose.log` contains `Running in KRaft mode...`; `experiments/flink_w1/docker-compose.yml` uses `apache/kafka:4.3.1` with broker and controller roles. |
| The Kafka Streams W5 run used Kafka in KRaft mode. | Supported | `experiments/results/kafka_streams_w5/docker-compose.log` contains `Running in KRaft mode...`; `experiments/kafka_streams_w1/docker-compose.yml` uses `apache/kafka:4.3.1` with broker and controller roles. |
| The Flink W5 run used Kafka in KRaft mode. | Supported | `experiments/results/flink_w5/docker-compose.log` contains `Running in KRaft mode...`; `experiments/flink_w1/docker-compose.yml` uses `apache/kafka:4.3.1` with broker and controller roles. |
| The Kafka Streams W1 run used `exactly_once_v2` and read-committed verification. | Supported | `experiments/kafka_streams_w1/src/main/java/bench/IdentityApp.java` sets `StreamsConfig.PROCESSING_GUARANTEE_CONFIG` to `StreamsConfig.EXACTLY_ONCE_V2`; `experiments/results/kafka_streams_w1/run_metadata.json` records `processing_guarantee: exactly_once_v2` and `consumer_isolation: read_committed`. |
| The Flink W1 run used Flink Kafka sink exactly-once delivery and read-committed verification. | Supported | `experiments/flink_w1/src/main/java/bench/FlinkIdentityJob.java` sets `DeliveryGuarantee.EXACTLY_ONCE`; `experiments/results/flink_w1/run_metadata.json` records `delivery_guarantee: exactly_once` and `consumer_isolation: read_committed`. |
| The Kafka Streams W2 run used `exactly_once_v2` and read-committed verification. | Supported | `experiments/results/kafka_streams_w2/run_metadata.json` records `processing_guarantee: exactly_once_v2` and `consumer_isolation: read_committed`. |
| The Flink W2 run used Flink Kafka sink exactly-once delivery and read-committed verification. | Supported | `experiments/results/flink_w2/run_metadata.json` records `delivery_guarantee: exactly_once` and `consumer_isolation: read_committed`. |
| The Kafka Streams W3 run used `exactly_once_v2` and read-committed verification. | Supported | `experiments/results/kafka_streams_w3/run_metadata.json` records `processing_guarantee: exactly_once_v2` and `consumer_isolation: read_committed`. |
| The Flink W3 run used Flink Kafka sink exactly-once delivery and read-committed verification. | Supported | `experiments/results/flink_w3/run_metadata.json` records `delivery_guarantee: exactly_once` and `consumer_isolation: read_committed`. |
| The Kafka Streams W4 run used `exactly_once_v2` and read-committed verification. | Supported | `experiments/results/kafka_streams_w4/run_metadata.json` records `processing_guarantee: exactly_once_v2` and `consumer_isolation: read_committed`. |
| The Flink W4 run used Flink Kafka sink exactly-once delivery and read-committed verification. | Supported | `experiments/results/flink_w4/run_metadata.json` records `delivery_guarantee: exactly_once` and `consumer_isolation: read_committed`. |
| The Kafka Streams W5 run used `exactly_once_v2` and read-committed verification. | Supported | `experiments/results/kafka_streams_w5/run_metadata.json` records `processing_guarantee: exactly_once_v2` and `consumer_isolation: read_committed`. |
| The Flink W5 run used Flink Kafka sink exactly-once delivery and read-committed verification. | Supported | `experiments/results/flink_w5/run_metadata.json` records `delivery_guarantee: exactly_once` and `consumer_isolation: read_committed`. |
| The artifact currently compares Apache Flink and Kafka Streams performance. | Not supported | Flink and Kafka Streams W1-W5 correctness have been executed twice per engine/workload, W1-W5 each have one host-side latency proxy run per engine, and W1-W5 have first three-rate proxy sweeps plus one 20/sec repeat. There is no calibrated throughput run, cost measurement, or fault experiment. |
| The artifact currently answers RQ1 throughput and p99 visibility-delay envelopes. | Not supported | W1-W5 have host-side p99 proxy sweeps at 10, 20, and 40 records/sec. Calibrated T0-T3 decomposed latency measurement is successfully implemented across all engines. Initial 2-minute backlog-growth checks at 100 events/sec are implemented for W1, but there is no full throughput envelope or long-duration stability run across W2-W5 yet. |
| The artifact currently answers RQ2 infrastructure cost distribution. | Not supported | No broker, checkpoint, changelog, network, or worker resource metrics exist yet. |
| The artifact currently answers RQ3 recovery and reprocessing behavior. | Not supported | No fault injection or engine recovery run exists yet. |
| Flink checkpointing is a valid mechanism to study for stateful fault tolerance. | Supported by reference | Apache Flink checkpointing documentation states that checkpoints recover state and stream positions to match failure-free execution: https://nightlies.apache.org/flink/flink-docs-stable/docs/dev/datastream/fault-tolerance/checkpointing/ |
| Kafka Streams changelog topics are a valid mechanism to study for state-store recovery. | Supported by reference | Apache Kafka Streams documentation describes internal changelog topics for state stores: https://kafka.apache.org/40/streams/developer-guide/manage-topics/ |
| Kafka 4.3.1 is the Kafka version targeted by the current Kafka Streams W1-W5 runs. | Supported by reference | Apache Kafka quickstart lists `apache/kafka:4.3.1` as the Docker image for the current Kafka release: https://kafka.apache.org/quickstart/ |
| Flink 2.2.0 is the Flink version used for the current Flink W1-W5 runs because of connector compatibility. | Supported by reference | Apache Flink downloads list Kafka Connector 5.0.0 compatibility with Flink 2.1.x and 2.2.x: https://flink.apache.org/downloads/ |
| Kafka Streams and Flink each held zero backlog growth across a 30-minute, 100 events/sec (180,000-event) open-loop run on W1, W2, W3, and W4. | Supported | `experiments/results/{kafka_streams,flink}_{w1,w2,w3,w4}_latency_stability_100/lag.csv` and `verification.json` (all eight pass with zero missing, unexpected, or duplicate records); `run_metadata.json` in each directory records `events: 180000`, `rate_per_sec: 100.0`. |
| Both engines' p99 engine-processing hop (`t2-t1`) jumps roughly a thousandfold from the stateless workloads (W1, W2) to the windowed workloads (W3, W4) under the same 30-minute sustained-load run, with Kafka Streams 23-42% higher than Flink on the windowed workloads. | Supported | `experiments/results/{kafka_streams,flink}_{w1,w2,w3,w4}_latency_stability_100/latency_summary.json`: Kafka Streams `p99_t2_t1_ms` is 6.0 (W1), 5.0 (W2), 6083.0 (W3), 6249.0 (W4); Flink is 4.0 (W1), 4.0 (W2), 4287.0 (W3), 5080.0 (W4). |
| Getting valid Flink W3/W4 30-minute stability numbers required fixing two bugs unrelated to engine behavior. | Supported | `docs/project_log.md` (2026-07-17) documents both: a host-bind-mounted `experiments/flink_w1/checkpoints/` directory never cleared between unrelated runs, causing a crash when resuming an incompatible checkpoint from a different workload; and a Kafka-consumer idle timeout hardcoded at 180 seconds in `scripts/run-flink-w1-latency.sh` regardless of run duration, too short for a 30-minute windowed run's silent period before its final output burst. Both fixed; first-attempt buggy runs are archived under `experiments/results/flink_w{3,4}_latency_stability_100_buggy_run*/` and not used as evidence. |
| For W3 tumbling_count at matched scale, raising the interval from 1000 ms to 10000 ms raises Kafka Streams' p99 processing hop 6.1x (3715.0 to 22616.0 ms) while its commit hop stays flat, and raises Flink's p99 commit hop 10.9x (271.8 to 2954.5 ms) while its processing hop stays flat. | Supported | Kafka Streams: `experiments/results/kafka_streams_w3_latency_tuning_control/latency_summary.json` (`p99_t2_t1_ms: 3715.0`, `p99_t3_t2_ms: 60.1`) and `experiments/results/kafka_streams_w3_latency_tuning_commit10s/latency_summary.json` (`p99_t2_t1_ms: 22616.0`, `p99_t3_t2_ms: 57.5`). Flink: `experiments/results/flink_w3_latency_tuning_control/latency_summary.json` (`p99_t2_t1_ms: 2655.0`, `p99_t3_t2_ms: 271.8`) and `experiments/results/flink_w3_latency_tuning_checkpoint10s/latency_summary.json` (`p99_t2_t1_ms: 2728.0`, `p99_t3_t2_ms: 2954.5`). All four runs matched 71/71 expected records with zero missing/unexpected/duplicate. Single run per configuration per engine; one workload. |
| A 180,000-event, 30-minute open-loop run at the W1-W4 stability parameters is not safe to run against W5 (stream_stream_join) without modification. | Supported | `experiments/results/kafka_streams_w5_latency_stability_100_incomplete/docker-compose.log` shows the container running from 2026-07-16T05:42 past 2026-07-16T16:48 (over 11 hours) without the correctness probe completing; the partial `expected.jsonl` it produced was 3.2 GB before being deleted. |
| A bounded 120-second, 12,000-event W5 stability run passes correctness for Kafka Streams and produces roughly 91x as many output records as input records. | Supported | `experiments/results/kafka_streams_w5_latency_stability_100/verification.json` reports `expected_count: 1093417`, `actual_count: 1093417`, zero missing/unexpected/duplicate, `passed: true`, from 12,000 input events; `latency_summary.json` reports p99 2913.538 ms. |
| Kafka Streams and Flink each produced correct W1 identity output during jvm_kill, broker_kill, and node_loss fault injection (six of six engine/failure-mode combinations). | Supported | `experiments/results/{kafka_streams,flink}_identity_latency_failure_{jvm_kill,broker_kill,node_loss}/verification.json` each report `expected_count: 2000`, `actual_count: 2000`, zero missing/unexpected/duplicate, `passed: true`. The Kafka Streams `node_loss` result is from a corrected retry; a first attempt (706/2000 matched) failed because of a script bug, fixed and documented in `docs/project_log.md` (2026-07-17); the buggy run is kept at `experiments/results/kafka_streams_identity_latency_failure_node_loss_buggy_run/` and is not used as evidence. |
| A Kafka Streams JVM kill and a Kafka Streams full node loss cost nearly the same p99 engine-processing hop (roughly 42.9 and 43.2 seconds) on the stateless W1 identity workload. | Supported with scope limit | `experiments/results/kafka_streams_identity_latency_failure_jvm_kill/latency_summary.json` reports `p99_t2_t1_ms: 42944.0`; `experiments/results/kafka_streams_identity_latency_failure_node_loss/latency_summary.json` reports `p99_t2_t1_ms: 43240.0`. Single run per failure mode; the workload is stateless, so this does not test whether the two failure modes diverge when there is real state to restore. |
| A Kafka Streams JVM kill on W1 identity adds roughly 42.9 seconds to the p99 engine-processing hop (`t2-t1`); an equivalent Flink JVM kill adds roughly 7.2 seconds. | Supported with scope limit | `experiments/results/kafka_streams_identity_latency_failure_jvm_kill/latency_summary.json` reports `p99_t2_t1_ms: 42944.0`; `experiments/results/flink_identity_latency_failure_jvm_kill/latency_summary.json` reports `p99_t2_t1_ms: 7232.0`. Single run per engine; not yet repeated or run on a stateful workload. |
| A Kafka broker kill adds roughly 8.5-8.8 seconds to the p99 ingestion hop (`t1-t0`) for both engines, then diverges: Kafka Streams' p99 commit-and-consume hop (`t3-t2`) absorbs roughly 9.4 more seconds, Flink's does not. | Supported with scope limit | `experiments/results/kafka_streams_identity_latency_failure_broker_kill/latency_summary.json` reports `p99_t1_t0_ms: 8454.993`, `p99_t3_t2_ms: 9387.298`; `experiments/results/flink_identity_latency_failure_broker_kill/latency_summary.json` reports `p99_t1_t0_ms: 8783.129`, `p99_t3_t2_ms: 1009.846`. Single run per engine. |
| Kafka Streams' windowed topologies (W3, W4) use `.suppress(Suppressed.untilWindowCloses(Suppressed.BufferConfig.unbounded()))` before output. | Supported | `experiments/kafka_streams_w1/src/main/java/bench/IdentityApp.java`, lines 114 and 155. |
| The commit interval (Kafka Streams) and checkpoint interval (Flink) were hardcoded at 1000 ms and are now configurable via environment variable, defaulting to the same 1000 ms used everywhere else in this paper. | Supported | `experiments/kafka_streams_w1/src/main/java/bench/IdentityApp.java` reads `COMMIT_INTERVAL_MS` (default `"1000"`); `experiments/flink_w1/src/main/java/bench/FlinkIdentityJob.java` reads `CHECKPOINT_INTERVAL_MS` (default `"1000"`); both compose files pass the variables through with the same default. |
| The artifact currently answers RQ3 recovery and reprocessing behavior for a full failure matrix on one workload. | Supported with scope limit | All six engine/failure-mode combinations (jvm_kill, broker_kill, node_loss, on both engines) are measured on W1 identity; no stateful workload or repeat trial exists yet. This narrows, but does not remove, the "Not supported" status recorded above for the full RQ3 answer. |
| Related Work positions this benchmark against five established prior stream-processing benchmarks with correct citations. | Supported by reference | Nexmark (Tucker, Tufte, Papadimos, Maier; OGI/OHSU technical report, ref 9); Yahoo Streaming Benchmark (Chintapalli et al., IPDPSW 2016, pp. 1789-1792, doi:10.1109/IPDPSW.2016.138, ref 10, verified via dblp `conf/ipps/ChintapalliDEFG16`); ESPBench (Hesse et al., ICPE 2021, doi:10.1145/3427921.3450242, ref 11); Theodolite (Henning, Hasselbring, Big Data Research 25:100209, 2021, doi:10.1016/j.bdr.2021.100209, ref 12); DSPBench (Bordin et al., IEEE Access 8:222900-222917, 2020, doi:10.1109/ACCESS.2020.3043948, ref 13, verified via dblp). Each venue/year/DOI checked against dblp or the publisher on 2026-07-17. |
| The Flink job's checkpoint-resume logic has no topology-compatibility guard and is only made safe by external harness cleanup. | Supported | `experiments/flink_w1/src/main/java/bench/FlinkIdentityJob.java` lines 50-80 scan `/tmp/flink-checkpoints` and resume from the highest `chk-N` across all job directories with no topology check; safety depends on the run scripts clearing the host-mounted checkpoint directory between runs. Documented as a Threat to Validity in `paper/final_paper.md`. Left as-is rather than modified because the Section 5.6 fault-injection numbers were produced by this exact code path and a change would require a full Flink image rebuild plus re-verification. |
| `scripts/run-failure-test.sh` uses `local` only inside functions, so it runs to completion under `set -euo pipefail`. | Supported | The former top-level `local start_ms=0` / `local keys=100` (which return exit 1 outside a function and aborted the script before any run launched) were replaced with plain assignments. Verified: `bash -n` passes and a stubbed dry run reaches the failure-injection stage past those assignments. |
| The Kafka Streams W2 30-minute stability directory is internally consistent (metadata, verification, and latency decomposition all describe the same 180,000-event run). | Supported | `experiments/results/kafka_streams_w2_latency_stability_100/` was regenerated 2026-07-17T19:57 by re-running the 180,000-event stability job; a prior state had `latency_summary.json`/`actual.jsonl`/`lag.csv` overwritten by an unrelated 12,000-event run while `run_metadata.json`/`verification.json` described the real 180,000-event run. Fresh `latency_summary.json` reports matched 89906, p99 1928.279, `p99_t1_t0_ms` 1000.495, `p99_t2_t1_ms` 5.0, `p99_t3_t2_ms` 1009.256; `verification.json` reports 89906/89906 with zero missing/unexpected/duplicate, passed true; `lag.csv` has 542 five-second samples. Paper §5.3 table and appendix A5 updated to these values (total 1958.2 -> 1928.3). |
