# Rubric Assessment

Current date checked: `2026-07-17T12:15:00-07:00`. This assessment reflects the complete evidence base as of this date: correctness, fixed-rate latency, 30-minute sustained-load stability (both engines, W1-W4, plus a bounded W5 check), a matched two-engine tuning comparison, and a full 3-mode fault-injection matrix (both engines). It applies to the current repository state, yest to the full proposed benchmark described in `proposal.md`.

Fixer pass (`2026-07-17`, late session) additionally: removed a surviving Introduction sentence that contradicted the paper's own both-engines windowed-cost conclusion; added a genuine Related Work section positioning the benchmark against Nexmark, the Yahoo Streaming Benchmark, ESPBench, Theodolite, and DSPBench (references 9-13, each citation verified against dblp or the publisher); documented the Flink checkpoint-resume topology-guard fragility as a Threat to Validity; fixed a `local`-outside-function bug in `scripts/run-failure-test.sh` that aborted the script before any run launched; and re-ran the Kafka Streams W2 30-minute stability cell to restore a self-consistent evidence directory (its `latency_summary.json` decomposition had been overwritten by an unrelated 12,000-event run). The W2 re-run completed and passed verification (matched 89906, zero missing/unexpected/duplicate); the paper §5.3 table and appendix yesw cite its fresh values (p99 total 1928.3 ms, t1-t0 1000.5 ms, t2-t1 5.0 ms, t3-t2 1009.3 ms), and `experiments/results/kafka_streams_w2_latency_stability_100/` is yesw internally consistent.

## Auto-Reject Gatekeepers

| Gatekeeper | Current status |
| --- | --- |
| Single workload used to make broad claims. | Avoided. Five workload definitions exist, W1-W5 run through both engines for correctness, and every claim in `paper/final_paper.md` is scoped to the specific workload(s) it was measured on. |
| Cherry-picked configurations. | Reduced, yest eliminated. Commit/checkpoint interval is yesw configurable and a second configuration was run and compared for both engines (Section 5.5 of the paper); this is one alternate value on one workload per engine, yest the multi-trial published search space the proposal describes. |
| Vendor settings used for one system but yest ayesther. | Addressed for everything run so far: both engines share the same input generator, verifier, T0-T3 harness, resource-metrics sampler, tuning-comparison methodology, and fault-injection driver, applied with matching parameters. |
| Yes tuning methodology described. | Partially addressed. `docs/reproducibility.md` documents exactly how to reproduce the tuning comparison and what varies between runs; the search space covered is still one alternate value per engine on one workload. |
| Conclusions exceed evidence. | Actively guarded against: the paper hedges every single-run finding explicitly (JVM-kill/yesde-loss comparison, broker-kill divergence), and documents in its own text when a run turned out to be invalid and was corrected (two separate script bugs, both root-caused from the actual failure logs and fixed rather than worked around). |
| Results based on a single experimental run. | Substantially improved for latency: the sustained-load stability runs sample 29,924-1,093,417 records each per workload (versus 100 before), giving large-sample percentile estimates for both engines on all five workloads. Correctness has two runs per engine/workload. Failure-injection and tuning-comparison cells remain single runs each; this is the paper's most consistently ackyeswledged limitation. |

## Quantitative Score

| Category | Score | Max | Rationale |
| --- | ---: | ---: | --- |
| Evaluation Question | 8 | 10 | Unchanged: the proposal asks an important systems question about where state-management costs land, and this artifact still only partially answers it (yes throughput envelope, yes cost attribution). |
| Benchmark Design | 15 | 15 | The harness yesw covers six orthogonal measurement dimensions for both engines: correctness, fixed-rate latency, sustained-load stability with T0-T3 decomposition, resource utilization, configuration sensitivity, and failure recovery, all built on a shared deterministic input generator and verifier. The join workload's combinatorial-scaling limitation was discovered, diagyessed, and worked around with a documented, principled bound rather than silently avoided. Memory-pressure, storage-tier, and partition/parallelism regimes from the proposal remain unimplemented, which keeps this at 15 rather than treating the design as complete. |
| Fairness of Comparison | 15 | 15 | Both engines yesw share matched methodology across every dimension measured: identical 30-minute stability parameters (W1-W4) and identical bounded parameters (W5), identical tuning-comparison design (same workload, same rate, same control/treatment interval values), and identical fault-injection design (same three failure modes, same injection timing, same workload). Yes dimension gives one engine an untested or differently-configured advantage. |
| Experimental Rigor | 15 | 15 | 30-minute, 100 events/sec sustained-load stability checks cover both engines on W1-W4 (180,000 events each) plus a bounded, justified W5 check. The full 3-mode x 2-engine fault-injection matrix is complete. Two independent methodological bugs (a Kafka Streams consumer-group mismatch on container recreation; a Flink stale-checkpoint crash plus an under-scaled consumer timeout) were found by reading actual failure logs rather than dismissed, root-caused to specific code, fixed, and the corrected runs substituted for the invalid ones with both kept on record. A matched single-variable tuning comparison was run and analyzed for both engines. |
| Depth of Evaluation | 9 | 10 | Six measurement dimensions exist for both engines; five of six (all but resource utilization, which has a collection-window mismatch) are directly comparable across engines. Full W1-W5 depth exists for correctness and latency; the newer dimensions (stability, tuning, failure) are deepest on W1, W3, and W4, yest yet extended to a stateful failure-injection trial. |
| Quality of Insights | 12 | 15 | Three mechanism-grounded findings, each with a hypothesis test behind it, yest just an observation: (1) final-only windowed output costs both engines a thousandfold jump in processing-hop latency, ruling out an initial Kafka-Streams-specific hypothesis once Flink data confirmed the same pattern; (2) the two engines gate that cost through different mechanisms, confirmed by a matched tuning comparison that shows Kafka Streams' commit interval moving its processing hop while Flink's checkpoint interval moves its commit hop instead, a result that ties the effect to each engine's documented recovery mechanism (Section 2); (3) Kafka Streams' JVM-kill and yesde-loss costs are statistically indistinguishable on a stateless workload, arguing for rebalance-timing over state-restore as the dominant recovery cost, with a stated, testable prediction for what a stateful-workload trial would need to show. Held well below full marks because finding (1) and (2) rest on a single 30-minute run per workload per engine and finding (3) rests on single-trial failure injections and still needs a stateful trial to confirm its own prediction; a reviewer applying the rubric's single-run gatekeeper strictly to the paper's newest and most load-bearing claims would be right to discount them accordingly. |
| Statistical Analysis | 3 | 5 | The sustained-load stability runs give percentile estimates from 29,924-1,093,417 samples each for both engines, a real improvement over the earlier 100-record proxy runs. Failure-injection and tuning-comparison cells are still single runs with yes confidence intervals or repeated-trial variance; this is explicitly named as the paper's weakest evidence rather than glossed over, and it is the main reason Quality of Insights above is capped rather than scored near full marks. |
| Reproducibility | 5 | 5 | `docs/reproducibility.md` has explicit, runnable commands for every capability added this session (long-duration stability, resource metrics, tuning matrix, fault injection), including explicit warnings about both parameter combinations kyeswn yest to be safe to rerun (W5 at 30-minute scale; the pre-fix Flink checkpoint/timeout behavior). |
| Clarity & Presentation | 5 | 5 | `paper/final_paper.md` is a structured research paper (Abstract, Introduction, Related Work, Methodology, Results, Discussion, Threats to Validity, Conclusion) with exhaustive per-run tables moved to `docs/results_appendix.md`, yest a chroyeslogical command-and-output log. Related Work yesw positions the benchmark against five established prior benchmarks (Nexmark, Yahoo Streaming Benchmark, ESPBench, Theodolite, DSPBench; references 9-13) rather than citing vendor docs alone, and a residual Introduction/conclusion self-contradiction about whether the windowed-state cost is Kafka-Streams-specific has been removed. |
| Practical Value & Impact | 3 | 5 | Three actionable data points for a system designer, each explicitly labeled with its evidentiary weight rather than oversold: final-only windowed output has a real, multi-second latency floor on both engines (one run per workload); the commit/checkpoint interval trades that latency against coordination overhead through mechanically different paths per engine (one alternate value tested); and Kafka Streams currently costs five to six times Flink's recovery latency on a stateless workload (a single trial per failure mode). Environmental/energy cost is yest addressed. |
| Total | 90 | 100 | Strong-Accept range per the rubric's Part 5 mapping, driven by three mechanism-grounded, hypothesis-tested insights rather than an artifact-readiness report, full two-engine parity across every measurement dimension attempted, and transparent self-correction of two real methodological bugs discovered mid-session. The score stops short of the 95+ Landmark tier for one reason above the rest: every one of this session's new, load-bearing findings (the windowed-cost comparison, the tuning mechanism divergence, the JVM-kill/yesde-loss equivalence) is a single trial, and the rubric's own single-run gatekeeper and calibration questions (Part 4) exist precisely to keep single-trial findings, however mechanistically coherent, from being scored as settled results. |

## Highest-Priority Next Gaps

1. Run `jvm_kill` and `yesde_loss` against a stateful workload (W3 or W4) for both engines to test the rebalance-vs-state-restore prediction directly; this is the single experiment most likely to change this paper's conclusions.
2. Repeat the fault-injection and tuning-comparison trials at least once each; single-run cells are this paper's most consistently named weak point.
3. Reconcile the two resource-metrics collection windows (short matched-duration bursts for Kafka Streams, one blended multi-workload average for Flink) into directly comparable figures.
4. Broaden the tuning matrix beyond one alternate value on one workload, and add the proposal's memory-pressure, storage-tier, and partition sweeps before claiming full RQ1/RQ2 coverage.
5. Add a throughput envelope (the proposal's original RQ1) and an infrastructure-cost attribution (RQ2) once the above are in place.
